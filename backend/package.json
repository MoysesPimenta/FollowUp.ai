{
  "name": "@followup/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "flake8 . && black --check . && mypy . && pydocstyle . && docformatter --check --recursive .",
    "test": "pytest ../tests"
  }
}
